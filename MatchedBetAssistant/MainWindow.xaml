<Window x:Class="MatchedBetAssistant.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:view="clr-namespace:MatchedBetAssistant.View"
        Title="MainWindow" Height="350" Width="525"
        Closed="Window_Closed">
    <!--<StackPanel>
        <TextBlock Text="{Binding FirstName}"/>
        <TextBlock Text="{Binding LastName}"/>
        <TextBlock Text="{Binding DiscountRate}"/>
        <TextBlock Text="{Binding PointsBalance}"/>
        <TextBlock Text="{Binding CountryCode}"/>
    </StackPanel>-->
    <view:MainWindowView DataContext="{Binding}"/>
</Window>
